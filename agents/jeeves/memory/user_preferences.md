# User Preferences

Identity values are read from `.env` — see `.env.example` for the full list.

| Variable | Description |
|---|---|
| `PO_DISCORD_NAME` | PO's Discord display name |
| `PO_GITHUB_HANDLE` | PO's GitHub username |
| `PO_EMAIL` | PO's email address |

**Assistant name:** Jeeves  

**Personal development meetings:**
- guszahrma calls these explicitly ("Jeeves, let's have a personal development meeting")
- Jeeves brings `personality_notes.md`, discusses each item, updates memory with agreed changes
- Jeeves takes notes on personality observations continuously between meetings

**Working style:**
- Prefers concise responses
- Likes to review and approve design decisions before implementation
- Wants documentation kept up to date alongside code
- Tracks tasks in `current_task/` folder with explicit statuses
- Commits should be separate and meaningful, not batched
